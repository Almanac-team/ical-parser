import * as fs from 'fs';

interface ICSFormat {
    
}

interface ActivityDefinition {
    activities: Array<any>
}

function parse(content: string): Array<ActivityDefinition> {
    const activities = []

    return []
}

export default parse